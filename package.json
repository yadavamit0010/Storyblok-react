{
  "name": "storyblok-react-boilerplate",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@storyblok/react": "^2.0.0",
    "react": "^18.1.0",
    "react-dom": "^18.1.0",
    "react-scripts": "^5.0.1",
    "tailwindcss": "^3.4.10"
  },
  "scripts": {
    "predeploy”: "yarn run build",
    "deploy": "yarn -d build -b main",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
